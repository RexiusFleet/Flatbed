# LOCAL TESTING ONLY — PostgREST pointed at the scratch test database.
db-uri = "postgres://authenticator:authenticator@localhost:5432/flatbed_test"
db-schemas = "public"
db-anon-role = "anon"
jwt-secret = "local-dev-jwt-secret-at-least-32-characters!!"
server-host = "127.0.0.1"
server-port = 3000
