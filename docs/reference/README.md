# Reference docs from the original (local, Python) repository

Copied unchanged for their business rules and history. File/route
references in them point at the old layout (`app/server.py`,
`app/web/js/*.js`); see `../ARCHITECTURE.md` for where each piece lives now.
The older decision-log archives they link to were not copied — they're in
the original repository.

- `original-CLAUDE.md` — rules, domain notes and the "Traps" list. Still the
  best guide to why the UI in `app.js` behaves the way it does.
- `decisions.md` — the current decision log (D250+).
- `design.md` — design tokens and UI conventions for `app.css`.
- `schema.md` — the data model explained.
- `supabase-server-port-plan.md` — the pre-port hosting/security plan.
